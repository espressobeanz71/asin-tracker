services:
  - type: web
    name: asin-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600 --workers 1
    envVars:
      - key: DATABASE_URL
        sync: false
